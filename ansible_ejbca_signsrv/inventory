---

all:
  children:
    certificate_authorities:
      hosts:
        ca-host:
          ansible_host: 107.22.132.198

    validation_authorities:
      hosts:
        va-host:
          ansible_host: 52.91.129.184
          
    registration_authorities:
      hosts:
        # ra-host:
        #   ansible_host: 54.82.38.202

    ejbca_servers:
      children:
        certificate_authorities:
        validation_authorities:
        registration_authorities:

    peer_servers:
      children:
        validation_authorities:
        registration_authorities:

  vars:
    ansible_user: ec2-user
    ansible_ssh_private_key_file: ~/.ssh/c2-ssh.pem
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

    